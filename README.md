# adk-web-project
1- eseguire dopo il download questi comandi:

pip install -r requirements.txt

2- fare downgrade a queste versioni:

pip install google-adk==1.19
pip install mcp==1.10
pip install pydantic==2.10

pip check 
# dovrebbe evidenziare nessun problema di dipendenze
